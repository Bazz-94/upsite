# Postgres connection string. Local for development, Neon in production.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/upsite?schema=public"

# A throwaway database for the Prisma integration test, which empties its tables.
# Leave it unset to skip that test. Never point it at the database above.
# TEST_DATABASE_URL="postgresql://postgres:postgres@localhost:5432/upsite_test?schema=public"
